# Schema package brief

Own:

- canonical runtime schemas;
- JSON Schema parity;
- generated public TypeScript types;
- validation diagnostics;
- cross-record integrity checks;
- category and collection schemas;
- valid/invalid fixtures.

This package must be framework-neutral and usable by tooling, package, API, website, and tests.
