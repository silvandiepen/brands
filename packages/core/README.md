# @open-brands/core

Environment-neutral brand lookup, resolution, search, filtering, color conversion, asset selection, and URL construction.

This package has no Vue, HTTP, Cloudflare, or filesystem runtime dependencies.
