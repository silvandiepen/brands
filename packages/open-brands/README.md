# open-brands

The public framework-neutral npm package for Open Brands.

It will expose typed metadata, resolution and search helpers, asset selection, CDN URL construction, per-brand exports, and design-token outputs without bundling every SVG in the root import.
