# API

Cloudflare Worker application for public metadata, asset resolution, image transformations, pack jobs, contribution sessions, validation jobs, and GitHub App integration.

Canonical brand data is compiled into release artifacts; D1 stores operational state only.
