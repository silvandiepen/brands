{
  "name": "@open-brands/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy --config wrangler.jsonc"
  },
  "dependencies": {
    "@open-brands/schema": "workspace:*",
    "@open-brands/core": "workspace:*",
    "@open-brands/tooling": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "wrangler": "^4.0.0"
  }
}
