# Website

Vue 3 and Vite application for `open-brands.org`.

The website uses generated release data for normal browsing, `@sil/ui` for shared primitives, `bemm` for app-specific classes, and the API only for dynamic operations such as packs and contributions.
