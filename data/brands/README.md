# Brands

Each brand uses the following structure:

```text
<brand-id>/
  brand.json
  assets/
    <asset-id>.svg
```

Directory names and manifest IDs must match. All colors and assets require source references.
