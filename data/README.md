# Canonical data

- `brands/` contains one reviewed directory per stable brand ID;
- `categories.json` defines the controlled taxonomy;
- `collections/` contains curated brand sets.

Generated aggregate data belongs in `packages/data` build output, not in this directory.
